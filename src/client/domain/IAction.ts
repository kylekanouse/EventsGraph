import ICallback from './ICallback'
/**
 * IAction
 * 
 * @description Main interface for actions that get associated to commands and are access through the ComandsController
 * @interface
 */

export default interface IAction extends ICallback {}