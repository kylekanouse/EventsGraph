import IEventData from "./IEventData"

/**
 * IEventsData
 *
 * @description interface for handling a grouping of EventsGraph events
 * @interface
 */

export default interface IEventsData {
  events: IEventData[]
}